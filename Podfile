# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PlantDetectSample' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PlantDetectSample
  pod 'plant-detect-sdk', :path => '../PlantDetect/ios/SDKOutput/'
  #pod 'plant-detect-sdk', :git => 'git@github.com:tespkg/plant-detect-sdk-ios.git', :tag => '1.0.1'
  
end
